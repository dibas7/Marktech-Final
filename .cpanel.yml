---
# Himalayan Host cPanel — Git deployment
# Deploys the pre-built SPA in public_html/ (generate with: npm run build:host)
deployment:
  tasks:
    - export DEPLOYPATH=/home1/marktech/public_html/
    - /bin/cp -R public_html/. $DEPLOYPATH
